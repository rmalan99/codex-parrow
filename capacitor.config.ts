import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.quizhelper',
  appName: 'ionic-quiz-helper',
  webDir: 'dist'
};

export default config;
